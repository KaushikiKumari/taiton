import 'package:flutter/material.dart';

class AppColors {
  static const darkTextColor = Color(0xFF283572);
  static const lightTextColor = Colors.black54;
  static const backgroundColor = Color(0xFF283572);
  static const appBarBackgroundColor = Colors.white;
  static const whiteTextColor = Colors.white;
  static const iconColor = Color(0xFF283572);
  static const whiteBoxColor = Colors.white;
  static const buttonColor = Color(0xFF283572);
}
